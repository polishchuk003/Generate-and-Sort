<Window x:Class="Brighteye.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Brighteye"
        mc:Ignorable="d"
        Title="Application for sorting numbers" Height="600" Width="600" WindowStartupLocation="CenterScreen" MinHeight="500" MinWidth="500">
    <Grid Background="SlateGray" Margin="10" > 
        <DataGrid Padding="5" Margin="5" Grid.Row="0" AutoGenerateColumns="False" ItemsSource="{Binding Items}">
            <DataGrid.Columns >
                <DataGridTextColumn Header="SortedNumber" FontSize="20" Binding="{Binding Value}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Background="Yellow" Grid.Row="1" Content="OK" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="30" Margin="20" Click="Button_Click"/>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
    </Grid>
</Window>
